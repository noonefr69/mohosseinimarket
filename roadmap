{
  /*
  Phase 1 — Foundation (you're almost done ✅)
├── ✅ Landing page
├── ✅ About / Contact
├── ✅ Auth (next-auth)
└── 🔲 Set up DB connection (Mongoose + Liara MongoDB)

Phase 2 — Catalog
├── Product model (name, price, stock, images, category)
├── Products listing page with filters
├── Single product page
└── Admin: add/edit/delete products (protect with role in next-auth session)

Phase 3 — Cart & Orders
├── Cart (start with localStorage → then save to DB for logged-in users)
├── Order model (user, items, total, status, paymentRef)
└── Checkout page (address form with zod/rhf, you already know this)

Phase 4 — Payment
├── ZarinPal request API route
├── ZarinPal verify API route
└── Order status update on success

Phase 5 — Polish & Deploy
├── Image hosting (Arvan Object Storage or Liara disk)
├── Deploy to Liara
└── Admin dashboard (orders, inventory)
  */
}
